<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.board.mapper.OperatorUserMapper">
	<resultMap id="operatorUserMap" type="com.example.demo.board.OperatorUser">
		<id column="id" property="id"/>
		<result column="username" property="username"/>
		<result column="email" property="email"/>
		<result column="password_hash" property="passwordHash"/>
		<result column="created_at" property="createdAt"/>
		<result column="updated_at" property="updatedAt"/>
	</resultMap>

	<select id="selectByUsername" resultMap="operatorUserMap">
		SELECT
			id,
			username,
			email,
			password_hash,
			created_at,
			updated_at
		FROM operator_user
		WHERE username = #{username}
	</select>

	<select id="insert" parameterType="com.example.demo.board.OperatorUser" resultMap="operatorUserMap">
		INSERT INTO operator_user (
			username,
			email,
			password_hash,
			created_at,
			updated_at
		) VALUES (
			#{username},
			#{email},
			#{passwordHash},
			now(),
			now()
		)
		RETURNING id, username, email, password_hash, created_at, updated_at
	</select>
</mapper>
